import { FC } from "react"
import { Range } from "../../Filter/Range"

export const PriceRangeList: FC = () => {
    return(
        <>
        <h4>Price range</h4>
        <Range/>
        </>
    )
}